# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.venv/
venv/

# Conda env living inside the repo (local dev only)
.conda/

# Environment
.env
.env.*
!.env.example

# Editor / OS
.vscode/
.idea/
.DS_Store

# Build artifacts
dist/
build/
*.log

# Supabase CLI
supabase/.branches/
supabase/.temp/

# fastText model — a local-only input to scripts/build_symbol_vectors.py
# (~7 GB, download to ~/Downloads — see docs/symbols.md § Symbol vectors).
# Never tracked: the built vectors under backend/app/data/symbol_vectors/ are
# the artefact, not the model.
cc.he.300.bin
*.bin
